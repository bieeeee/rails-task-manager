<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>
<p>completed? <%= @task.completed %></p>

<%= link_to 'Back to list', tasks_path %>
<%= link_to 'Edit', edit_path %>
<%= link_to 'Delete', task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Sure?"} %>
